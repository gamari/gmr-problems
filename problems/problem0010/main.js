function listMondays(startDateStr, endDateStr) {
    const startDate = new Date(startDateStr);
    const endDate = new Date(endDateStr);
    const mondays = [];

    // TODO 曜日を列挙する処理を実装してください
}

listMondays("2023-01-01", "2023-01-31");
